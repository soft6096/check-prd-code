---
name: check-prd-code
description: >
  核对需求文档（PRD）与代码是否对得上，产出「待修复」「待确认」两份报告，适用于 Java 工程。
  当用户要核对需求与代码、检查需求有没有漏做、判断 AI 生成的代码是否偏离需求时使用。
---

# check-prd-code 操作说明

你是这套工具的**判断环节**。脚本负责翻代码、检索、核对、验真，
你只负责它做不了的那件事：**读懂代码逻辑，判断需求到底实现了没有。**

## 适用范围

- 只支持 **Java 工程 + MyBatis XML**；其它语言工程不适用。
- 需要用户提供：PRD 文件（.md）和代码目录。

## 脚本在哪

脚本与本文件同目录。下面命令里的 `<SKILL_DIR>` 指本 skill 所在目录
（即 SKILL.md 的父目录），执行时替换成实际绝对路径：

```bash
python3 "<SKILL_DIR>/check_prd_code.py" extract --prd <PRD 文件或目录>
python3 "<SKILL_DIR>/check_prd_code.py" compare --code <代码目录>
python3 "<SKILL_DIR>/check_prd_code.py" verify  --code <代码目录>   # 可选，对抗复核
python3 "<SKILL_DIR>/check_prd_code.py" report  --code <代码目录> --out <报告目录>
python3 "<SKILL_DIR>/check_prd_code.py" status
```

## 分工

- 能用「搜得到 / 搜不到」回答的 → 脚本自己判，不会找你
- 必须读懂意思才能判的 → 脚本打包成任务包丢给你
- 谁都不能替你决定的 → 直接进「待确认」，交给人

## 流程总览

```
① 拆解 PRD
   [脚本]          切块 → 按"有没有图"分两类 → 停下来
   [你·多模态]     图片批：看图提炼字段 / 状态 / 规则
   [你·普通模型]   文本批：读正文提炼规则
   [脚本]          合并成 items.json

② 比对代码
   [脚本]          建索引 → 静态判定 → 生成判定任务包
   [你]            判断"这条规则实现了没有"（纯文字，不需要看图）

②.5 对抗复核（可选）
   [脚本]          挑出判成"已实现 / 已偏离"的条目 → 生成复核任务包
   [你·换个模型]   专门去推翻这些结论；推不翻的才留下

③ 出报告
   [脚本]          验真 → 应用复核结论 → 渲染两份报告
```

你出场两次，分别在 ① 和 ②。**只有 ① 的图片批需要能看图的模型**，
其余环节普通模型就够 —— 不要拿多模态模型去跑纯文本的活，贵且没必要。

另外要清楚一件事：**"这块是不是服务端"只有你判断得了。**
脚本能判断"这块有没有图"，但判断不了"这块讲的是前端还是服务端"。
所以每一块内容都要你按下面铁律二筛一遍。

## 模型怎么选

**一句话规则：要读图就用多模态，不读图就用 DeepSeek。**

| 环节 | 要读图吗 | 推荐 |
| :--- | :--- | :--- |
| PRD 切块、建索引、验真、出报告 | — | 脚本，不用模型 |
| **图片批**（页面原型图） | **要** | **GLM-5V-Turbo**（智谱多模态，原生读图） |
| 文本批（正文） | 不要 | **DeepSeek** |
| 代码判定 | 不要 | **DeepSeek** |
| 判定有分歧要复核 | 不要 | 换更强的模型（少量、要深思） |

### 每次开始一批之前，停下来问用户

**不要长篇解释**，就三行：

```
这批：图片批-003（6 张原型图）
要读图 → 推荐 GLM-5V-Turbo
切好模型说「继续」
```

文本批同理：

```
这批：文本批-002
纯文字 → 推荐 DeepSeek
切好模型说「继续」
```

**然后等用户回复再动手。**

不要自己判断"当前模型好像能读图就直接开始" —— **用什么模型是用户的决定，不是你的。**
你读得出来，不代表用户想让你用这个模型跑（可能贵、可能他要统一管理）。
停下、问一句，成本是几秒钟；跑错了，是几十批重做。

## 三个环节

### 环节一：把需求改写成"能判对错的话"

跑完 `extract` 之后，它会**停下来**，告诉你任务包分成了两批：

```
① 图片批   .checkprd/extract/任务/图片/图片批-*.md
   每块一张页面原型图，需求藏在图里
   → 必须用【能看图的模型】，结果写到 .checkprd/extract/结果/图片批-*.json

② 文本批   .checkprd/extract/任务/文本/文本批-*.md
   → 普通够用便宜的模型即可，结果写到 .checkprd/extract/结果/文本批-*.json
```

**两批必须分开跑、分开换模型**，原因：图片批每块只有一句标题 + 一张原型图，
文字几乎没有，看不到图 = 这部分需求等于没读；但图片批只占少数块，
没必要让全部任务都走贵模型。

---

#### 铁律一：上下文在任务包里，不在你的"记忆"里

批次是按体量切的，**一个批次就是一个独立对话** —— 你看不到其他批次的内容。
所以别指望"记得前面读过什么"，那必然漏。**上下文得由任务包塞到你手上。**

任务包已经替你把两件事做好了：

1. **每个块都带完整标题路径**（如 `页面详细设计 / 7.1 [后台-厂端] 商品主数据管理列表页`），
   这是它在文档里的位置
2. **每批开头附了「基础定义」章节** —— 数据字段定义、系统数据字典、
   核心业务逻辑规则、核心业务流程。这些是总纲，后面每个页面都引用它们。

**你要做的**：把开头那段「基础定义」当成"已经读过的前文"来用。

- 看到「状态变更为 X」→ 回字典里找 X 的定义
- 看到字段名 → 对照字段定义，别自己编一个类型或约束

**查不到怎么办**：在条目的 `note` 里写一句「依赖 XX 定义，本批未见」，
**不要自己猜一个**。查不到是信息，猜一个是污染。

#### 铁律二：只管服务端，前端和客户端的东西一律不管

这份 PRD 里混着**服务端、后台前端、小程序端、APP 端**的需求。
这些条目最终是拿去跟**后端代码**比对的，所以**只有服务端能落地的才值得提取**。

**注意：这条对文字和图片都成立** —— 不只是图片，前端性质的文字段落同样跳过。

按**内容**判断，不按"端"判断：

| 内容 | 提取吗 | 提出来能对到什么 |
| :--- | :--- | :--- |
| 字段、表结构、字段约束（长度 / 必填 / 唯一） | **提取** | Entity、DDL |
| 业务规则、校验规则、计算口径 | **提取** | Service 逻辑 |
| 状态定义、流转条件、触发时机 | **提取** | 状态机、枚举 |
| 接口路径、入参出参、错误码 | **提取** | Controller、DTO |
| 字典与枚举值 | **提取** | 字典项 |
| 事务、幂等、补偿、定时任务 | **提取** | 服务端实现 |
| 页面布局、区块位置、间距字号 | 跳过 | 前端 |
| 按钮摆哪、什么颜色、什么图标 | 跳过 | 前端 |
| 页面跳转、前端路由 | 跳过 | 前端 |
| 弹窗动效、加载态、埋点 | 跳过 | 前端 |
| 小程序 / APP 的原生交互 | 跳过 | 客户端 |
| 背景、价值、术语、变更记录 | 跳过 | 不是需求 |

**图片同样按内容判断**，一句话判断法：

> 这张图能不能提炼出「代码里找得到的名字或规则」？
> 能（字段名、状态值、接口路径、校验规则）→ 解析；
> 不能（只有位置、样式、交互感）→ 跳过，**不要为了凑数硬造条目**。

一个实用信号：小节标题叫「页面布局」的多半是线框图，
叫「各区域元素与交互」的多半有字段和规则。
**但这只是提示，最终看内容** —— 有的布局图里也标了字段名。

#### 别搞一刀切：同一块里常常混着两种内容

页面设计的章节里，服务端规则和前端描述是掺在一起的：

```
【新增商品】
  - 商品名称：必填，最长 60 字        ← 服务端要校验 → 提取
  - 点「保存」，按钮固定在右下角        ← 前端 → 跳过
  - 同一分类下商品名不能重复            ← 服务端唯一约束 → 提取
```

**同一块里既有要的也有不要的**：把服务端那部分拆成条目，前端那部分不要。
不要因为"这块整体像前端"就整块扔掉，也不要因为"有字段"就把按钮位置也抄进来。

#### 跳过的一定要记下来

不管跳的是文字还是图片，都在 `skipped.md` 里记一行，写明是**看过之后主动跳过**的：

```
7.1.1 页面布局                      —— 纯前端布局描述
7.1.2 【新增商品】保存按钮           —— 按钮位置与样式
图片批-001 / B0131 / image 79.png   —— 纯页面布局线框图
```

**漏提取比错提取危险** —— 错了能发现，漏了永远没人知道。
跳过的必须显式写出来，让人有机会反驳。

---

#### 开始处理前，先确认模型

处理每一批之前，**先跟用户说一句**，让他确认当前模型对不对：

> 这批是【图片批】，需要能看图的模型。当前如果不是多模态模型，请先切换。

**如果用户没切模型就让你处理图片批，不要硬着头皮做。**
看不到图就照标题瞎猜，产出的条目全是错的 —— 这比不做还糟，因为错的条目
后面会拿去跟代码比对，制造一堆假问题。

先拿一张图试：用文件读取工具打开任务包里给的图片路径。

- 读出了图里的内容 → 可以继续
- 读不出（报错、或只拿到二进制/乱码）→ 停下来告诉用户：
  「当前模型读不了图，处理图片批会瞎猜，请先切到多模态模型」

#### 处理细节

**图片批**：任务包里每块都给了图片的**绝对路径**，用文件读取工具逐个打开。
建议一个文件开一个对话 —— 一次塞十几张图容易读不完。

**文本批**：正常读，按块改写成断言，但同样按铁律二筛。

## 全部批次写完之后

重跑同一条命令：

```bash
python3 check_prd_code.py extract --prd <同一个 PRD>
```

脚本会自动合并成 `items.json`，并告诉你还差哪几批。

> 如果用户跳过了图片批就要继续，**必须明确告诉他**：
> 图片里的需求没被收进条目，后面的报告会出现成片误报。
> 脚本拦不住这个，但你要说。

要点：

- 每条必须是**一句能判对错的话**。「提升用户体验」这种判不了对错的，不要产出条目。
- `kind` 字段定生死，只能填两个值：
  - `existence` —— 能用「搜得到 / 搜不到」回答的，比如「接口 `/refund/withdraw` 存在」
  - `behavior` —— 要读懂代码逻辑才知道的，比如「撤销必须按批次整批原子关闭」
  - **标错了两头都浪费**：`existence` 会被脚本直接判掉，`behavior` 才会交到你手上。
- 一条断言只判一件事。「接口 X 存在，且入参必填 Y」这种要拆成两条。
- 拆不动的（背景、术语、流程图说明、写得含糊的）**不要硬凑成假的可验证断言**，
  老老实实记进 `skipped.md`。**漏拆比错拆危险**——错了能发现，漏了永远没人知道。

**完成判据**：每个内容块都过了一遍；`items.json` 的 `id` 从 R-001 连续编号；
拆不动的全部在 `skipped.md` 里逐条注明原因。

### 环节二：判断每条需求实现了没有

跑完 `compare` 之后，读：

```
.checkprd/compare/tasks/batch-NNN.md
```

每个任务包里都有：需求条目、需求出处、脚本找到的候选位置、相关代码片段。
把结果写回同名的 json：

```
.checkprd/compare/tasks/batch-NNN.json
```

判定值只有五个：

| verdict | 什么时候用 |
| :--- | :--- |
| `implemented` | 代码里确实做了 |
| `partial` | 做了一部分，还缺 |
| `missing` | 完全没做 |
| `deviated` | 做了，但跟需求说的不一样 |
| `undecidable` | 给的代码不够判断 |

**四条硬规矩：**

1. `quote` 一律从代码里**逐字照抄**：原样复制，不加省略号、不改写、不概括。
   脚本会逐字比对，对不上这条判定当场作废，转进「待确认」。
2. `quote` 只写代码本身，**不带行号前缀**（脚本会剥掉行号再比，带了更容易出错）。
3. `file` 用任务包里给的相对路径；`line` 用代码块左边标着的那一列数字。
4. 判不了就写 `undecidable`：**宁可老实说"判不了"，也不编一个 `implemented`**。

关于 `missing`：判它的时候不需要给引用（本来就没东西可引）。
但**如果你能指出相关代码在哪、缺的是哪一段，就一定要写进去** ——
脚本会拿这个决定这条是"能直接改"还是"要人先看一眼"。

**完成判据**：`tasks/` 里每个 `batch-NNN.json` 都已回填；每条 `verdict` 五选一；
所有 `implemented / partial / deviated` 的 `quote` 都是逐字原文。

### 环节二·补（可选）：对抗复核

第一遍判定只证明了"引用的代码真实存在"，没证明"这段代码正好是需求说的那件事"。
所以可以加跑一步 `verify`，专门去推翻"已实现 / 已偏离"的结论：

```bash
python3 check_prd_code.py verify --code <代码目录>
```

它会：

1. 挑出判「已实现 / 已偏离」、引用已验真、且是行为类的条目；
2. 连同脚本给出的代码窗口（引用所在方法体 + 本批共用代码），打包成
   `.checkprd/verify/tasks/batch-NNN.md`；
3. 等你把复核结果写回同名 `.json` 后，`report` 会自动应用：
   推不翻的照旧，被推翻或验不过的**降级进「待确认」**。

**这一步是可选、建议开着的**：它专治"拿一句放到哪都成立的代码（如 `return Result.ok();`）
冒充与需求相关"的假实现。跑它要注意两点：

- **换一个模型**：复核必须由**与第一遍不同的强推理模型**来做。同一个模型很难推翻自己。
- **只能引用脚本给的代码**：复核模型引用的每一行都必须落在任务包给出的代码窗口内，
  越界引用会被判无效，该条降级。所以别去引用任务包里没有的代码。

**复核结果的判定值**（写回 `.checkprd/verify/tasks/batch-NNN.json`）：

| second_verdict | 含义 |
| :--- | :--- |
| `confirmed` | 原判成立（必须逐字引用代码，且把需求拆成子条件逐条给位置） |
| `refuted` | 原判不成立（说明第一遍引用的代码为什么答非所问） |
| `partial` | 只满足一部分 |
| `unverifiable` | 看不到足够代码；拿不准就写它，会进「待确认」 |

**完成判据**：`.checkprd/verify/tasks/` 里每个 `batch-NNN.json` 都已回填；
判 `confirmed` 的每条都给了子条件（clauses）与对应代码位置。

### 环节三：交报告

跑完 `report` 之后，把这两份文件交给用户：

```
01-待修复.md     证据齐全，可以照着改
02-待确认.md     需要人拍板，不要自作主张执行
```

如果用户接着说"帮我改"，**最多只能动 `01` 里的条目**，
并且改完要重跑一遍三步，确认那些条目消失了。

**完成判据**：两份报告都已交给用户，并说明 01 可直接执行、02 需人先勾选。

## 硬性要求

- 每个判定都要有代码原文撑着；拿不出原文，就写 `undecidable`。
- 只执行 `01-待修复.md` 里的条目；`02-待确认.md` 等用户勾完再动。
- 报告里的判定和位置由脚本写；你只在"依据"栏补自己的判断。
- 只依据 PRD 写到的内容判定；PRD 没写的（如"一般都要做参数校验"）不进报告。

## 安装

本 skill 的脚本与 `SKILL.md` 同目录，**安装时要把整个目录放进去** ——
只软链 `SKILL.md` 会让命令找不到脚本。

```bash
# 方式一：整目录软链（推荐，git pull 可同步更新）
./install.sh                       # 自动挑选已存在的 skills 目录
./install.sh ~/.claude/skills      # 或显式指定目标目录

# 方式二：手动整目录软链
ln -s "$(pwd)" ~/.agents/skills/check-prd-code
```

常见的 skills 目录：`~/.agents/skills`、`~/.config/opencode/skills`、
`~/.claude/skills`、`~/.codebuddy/skills`。
