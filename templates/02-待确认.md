# 待确认

这些地方我拿不准，得你定一下。分两种：一种代码里有、需求里没提；另一种是需求自己写得对不上。

**两种用法，挑一个**：

- 让 AI 读这份文档，它会一条条弹给你选
- 或者你自己在下面勾，勾完把文档扔给 AI，它照着你的选择去改

一共 5 条。

---

## 一、代码里有，需求里没提的

> 只列对外能看到的东西：接口、字段、枚举、定时任务。
> 内部的工具类、日志、配置这些不算多余，没往里放。

### 1. 多了一个手动重试接口

`RetryController.java:41` 有个 `POST /internal/retry`，需求文档从头到尾没提过。

看起来像是给运维排障留的口子。

- [ ] 需求本来就不需要 → 删掉
- [ ] 需求漏写了 → 我去补需求文档，这条不用管
- [ ] 是通用能力 → 保留，以后别再报出来

处理人 / 日期：

### 2. 订单明细里多返回了两个字段

`OrderDetailRespDTO.java:63` 返回了 `channelCode` 和 `channelName`，需求里查不到出处。

- [ ] 前端要用，保留
- [ ] 不用了 → 删掉
- [ ] 需求漏写 → 我去补文档

处理人 / 日期：

### 3. 多了一个支付状态同步的定时任务

`PaymentStatusSyncServiceImpl.java:105` 有个循环同步的任务，需求里没写。

- [ ] 是补偿机制，保留
- [ ] 不需要 → 删掉
- [ ] 需求漏写 → 我去补文档

处理人 / 日期：

---

## 二、需求自己写得对不上的

### 4. 退货地址到底什么时候返回，两处说法打架

`需求文档.md:112` 说「仅 `SHIPPED` 状态返回退货地址」，
`需求文档.md:203` 又说「详情页始终展示退货入口」。

代码现在恒返回 `null`（`RefundServiceImpl.java:1415`），两边都没满足。改之前得先定一个准。

选一个：

- [ ] 按 112 行：只有 `SHIPPED` 状态才返回
- [ ] 按 203 行：不管什么状态都返回

顺带定一下：截止时间按订单签收起算 7 天，还是按申请时间起算？（需求两处都没写）

处理人 / 日期：

### 5. 退款资格的有效期，两个数对不上

`需求文档.md:88` 写的是签收后 7 天内可申请，
`需求文档.md:132` 写的是 15 天。

代码 `RefundEligibilityServiceImpl.java:560` 用的是 7 天。

- [ ] 就是 7 天，132 行是笔误 → 我改文档
- [ ] 应该是 15 天 → 改代码

处理人 / 日期：

---

## 勾完怎么办

勾完直接把这整份文档给 AI 就行，它能看懂你勾了哪些，按你的选择去改。

「需求漏写」和「保留」这两类不会产生代码改动，AI 会自动跳过。
标了「删掉」和「改代码」的，AI 会动手。
